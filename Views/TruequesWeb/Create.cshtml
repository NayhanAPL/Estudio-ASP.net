@model versión_5_asp.Models.Trueque

@section css {
<link href="@Url.Content("~/css/login.css")" rel="stylesheet" />
}

<main role="main" class="app-content px-3" style="padding: calc(1rem*5); ">
    <div class="container d-flex justify-content-center align-items-center">
        <div class="line-length-sm">
            <article class="card py-5">
                <div class="pb-4">
                        <h2>Crear Trueque</h2>
                    </div>
                <div style="text-align: left;">
                    <div asp-validation-summary="All" class="text-danger"></div>
                    <form asp-action="Create" asp-route-returnUrl="@Context.Request.Path" enctype="multipart/form-data">
                        <div class="margin-bottom-2xl">
                            <div>
                                <label class="form-label">Categor&iacute;a</label>
                            </div>
                            <div>
                                <select asp-for="Type" class="form-control" asp-items="Html.GetEnumSelectList<versión_5_asp.Models.Type>()"></select>
                            </div>
                        </div>

                        <div class="margin-bottom-2xl">
                            <label class="form-label" asp-for="Proposition" class="control-label"></label>
                            <input asp-for="Proposition" class="form-control" />
                            <span asp-validation-for="Proposition" class="text-danger"></span>
                        </div>
                        <div class="form-group margin-bottom-2xl">
                            <label class="form-label" asp-for="Search"></label>
                            <input asp-for="Search" type="text" class="form-control">
                            <span asp-validation-for="Search" class="text-danger"></span>
                        </div>
                        <div class="form-group margin-bottom-2xl">
                            <label class="form-label" asp-for="ExtraInfo"></label>
                            <input asp-for="ExtraInfo" type="text" class="form-control">
                            <span asp-validation-for="ExtraInfo" class="text-danger"></span>
                        </div>
                        <div class="form-group margin-bottom-2xl">
                            <label class="form-label" asp-for="Image.ImageFile" class="form-label"></label>
                            <div>
                                <input asp-for="Image.ImageFile" class="form-control" type="file" id="formFile" onchange="preview()">                                                             
                                <button id="trash-btn" style="width:100%;" onclick="clearImage" class="btn bg-orange text-white my-3"><i class="fa fa-trash-alt"></i></button>
                                 <img class="img-fluid rounded mx-auto d-block" id="frame" src="" style="width:204px;height:auto;"/>
                            </div>
                             
                        </div>

                        <button class="btn bg-orange text-white btn-lg" style="width:100%;" type="submit">Crear</button>
                    </form>
                </div>
            </article>
        </div>
</main>

@section Scripts {
    @{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}
<script>
    function preview() {
        frame.src = URL.createObjectURL(event.target.files[0]);
         //document.getElementById('trash-btn').show();
    }
    function clearImage() {
        document.getElementById('formFile').value = null;
        frame.src = "";
    }   
</script>
}
